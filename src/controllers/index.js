const Authenticate = require('./auth/Authenticate');
const Other = require('./auth/Other');
const CreateCollection = require('./collections/CreateCollection');

module.exports = {
    Authenticate,
    Other,
    CreateCollection
}